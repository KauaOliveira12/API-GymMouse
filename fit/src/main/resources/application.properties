spring.application.name=fit

# URL Interna formatada para JDBC (sem usuário e senha na string, e com a porta 5432)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d9fd0drtqb8s73cr8v3g-a:5432/streaks_db_wu58}

# Usuário e Senha
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:streaks_db_wu58_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:lQCqdExTHFmwVm1moQapvYTsnXyCN1zZ}

# Dialeto do Hibernate para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Permite salvar foto de perfil em base64 no JSON (pode passar de 1MB)
server.tomcat.max-http-form-post-size=15MB
server.tomcat.max-swallow-size=15MB
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=15MB

# Envio de e-mail (recuperação de senha). Configure no Render/host com variáveis de ambiente.
# Exemplo Gmail: host=smtp.gmail.com, port=587, username=seu@gmail.com, password=senha-de-app
app.mail.enabled=${APP_MAIL_ENABLED:false}
app.mail.from=${APP_MAIL_FROM:comercialstreaks@gmail.com}
spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:comercialstreaks@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:ttkkybeeznwzwapw}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true